name: Test

on: ["push", "pull_request"]

jobs:
  test:
    name: Run unit tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Install
        run: npm ci --ignore-scripts
      - name: Install Playwright browsers
        run: npm exec --no --ignore-scripts -- playwright install chromium --with-deps
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
